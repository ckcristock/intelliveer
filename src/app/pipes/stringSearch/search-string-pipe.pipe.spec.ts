import { SearchStringPipePipe } from './search-string-pipe.pipe';

describe('SearchStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
